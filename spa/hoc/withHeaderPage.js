import React from 'react';
import ReactDOM from 'react-dom';

const withHeaderPage = (wrappedComponent, information) => {
  class HeaderPage extends React.Component {
    render() {
      const { information, componentStyle } = this.props;
    }
  }
};
